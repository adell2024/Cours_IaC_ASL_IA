# 🧪 Exercice 04 — ConfigMaps & Secrets

## 🎯 Objectif
Apprendre à **gérer la configuration et les secrets** dans Kubernetes afin que les applications restent **découplées de leur configuration** et que les informations sensibles soient protégées.

## 📌 Contexte
- Les pods doivent souvent utiliser des **fichiers de configuration**, **variables d’environnement**, ou **mots de passe**  
- Kubernetes propose deux objets principaux pour cela :
  1. **ConfigMap** → stocke des informations de configuration non sensibles
  2. **Secret** → stocke des informations sensibles (mots de passe, clés API, certificats)  

## 🧩 Étapes

### Étape 1 — Créer un ConfigMap

Exemple : configuration d’un message de bienvenue pour NGINX

kubectl create configmap nginx-config --from-literal=welcome_message="Bienvenue sur mon cluster Kubernetes !"

Vérifier :

kubectl get configmap nginx-config -o yaml

### Étape 2 — Injecter le ConfigMap dans un pod

Créer un pod test utilisant le ConfigMap comme variable d’environnement. créer le fichier pod-configmap.yaml :
apiVersion: v1
kind: Pod
metadata:
  name: nginx-config-test
spec:
  containers:
  - name: nginx
    image: nginx
    env:
    - name: WELCOME_MESSAGE
      valueFrom:
        configMapKeyRef:
          name: nginx-config
          key: welcome_message

Déployer :

kubectl apply -f pod-configmap.yaml

Vérifier le pod et sa variable d’environnement :

kubectl exec -it nginx-config-test -- printenv WELCOME_MESSAGE

